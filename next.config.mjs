/** @type {import('next').NextConfig} */
const nextConfig = {
    metadata: {
      title: "My Bookshelf App",
      description: "A bookshelf app built with Next.js and Redux",
    },
  };
  
  export default nextConfig;